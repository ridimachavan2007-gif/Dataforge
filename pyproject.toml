[project]
name = "switchvoice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shravanihanbar", email = "shravani.hanbar25@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fasttext-community>=0.11.7",
    "huggingface-hub>=1.30.0",
    "livekit-agents[deepgram,openai,rime]~=1.7",
    "python-dotenv>=1.2.3",
]

[project.scripts]
switchvoice = "switchvoice:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
